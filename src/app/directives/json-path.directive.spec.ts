import { JsonPathDirective } from './json-path.directive';

describe('JsonPathDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonPathDirective();
    expect(directive).toBeTruthy();
  });
});
