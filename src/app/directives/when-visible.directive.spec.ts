import { WhenVisibleDirective } from './when-visible.directive';

describe('WhenVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new WhenVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
